@model IEnumerable<YtBookStore.Models.Domain.Genre>
@{
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" href="/Genre/Add">Ekle</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <h3>DERSLER</h3>

            <table  class="table table-stripped table-bordered table-dark">
                <thead>
                    <tr>
                        <th>Kayıtlı dersler</th>
                        <th>Değiştirmek için tıkla</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model){
                    <tr>
                        <td>@item.Name</td>
                        <td>
                                <a class="btn btn-success" href="/Genre/Update?id=@item.Id"><i class="bi bi-pen"></i> Düzenle</a> |
                                <a class="btn btn-danger" onclick="return window.confirm('Are you sure?')" href="/Genre/Delete?id=@item.Id"><i class="bi bi-trash"></i> Sil</a>
                               
                        </td>
                    </tr>
                    }
                    
                </tbody>
            </table>
        </div>
    </div>
</div>

